import { breakPoints } from './setting/breakPoints';
import { colors } from './setting/colors';
import { fonts } from './setting/fonts';
import { globalStyle } from './setting/globalStyle';

export const theme = {
  breakPoints: breakPoints,
  colors: colors,
  fonts: fonts,
};

export const GlobalStyle = globalStyle;
